# React Client Environment Variables Template
# In local development with Vite, leave VITE_API_URL empty so requests route through the Vite proxy (/api -> http://localhost:5000)
# In production, set this to your hosted backend API URL (e.g., https://api.yourdomain.com)
VITE_API_URL=
